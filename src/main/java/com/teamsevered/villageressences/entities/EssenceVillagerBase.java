package com.teamsevered.villageressences.entities;

import net.minecraft.entity.EntityType;
import net.minecraft.entity.merchant.villager.VillagerEntity;
import net.minecraft.entity.merchant.villager.VillagerProfession;
import net.minecraft.entity.villager.IVillagerType;
import net.minecraft.util.text.StringTextComponent;
import net.minecraft.world.World;

public class EssenceVillagerBase //extends VillagerEntity
{
    public EssenceVillagerBase(String name, EntityType type, World world, VillagerProfession proffession, IVillagerType villagerType, String profession)
    {
       // super(type, world);
       // setCanPickUpLoot(true);
       // getNavigator().setCanSwim(true);
       // setVillagerData(getVillagerData().withType(villagerType).withProfession(profession));
      //  setCustomName(new StringTextComponent(name));
      //  ProfessionHandler.PROFESSIONS.add(profession);
     //   EntityHandler.ENTITIES.ADD(this);
       // newProfession = new Villager();
       // EntityRegistry
    }


}
